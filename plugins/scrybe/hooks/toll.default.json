{
  "$comment": "Shipped defaults for the Scrybe search guard. Rule: deny only a keyword search over issues; listing is never blocked. Do NOT edit this file \u2014 it is overwritten on every plugin update. Copy the keys you want into <SCRYBE_DATA_DIR>/toll.json instead; that file wins.",
  "enabled": true,
  "lower_seconds": 15,
  "upper_seconds": 30,
  "scope": "global",
  "window": "fixed",
  "marker_dir": null,
  "note_once_per_session": true,
  "guards": [
    {
      "id": "gh-issue-list",
      "action": "auto",
      "pattern": "\\bgh\\s+issue\\s+list\\b",
      "hint": "search_knowledge over this repo's issue source finds the duplicate that words it differently"
    },
    {
      "id": "gh-search-issues",
      "action": "auto",
      "pattern": "\\bgh\\s+search\\s+issues\\b",
      "hint": "search_knowledge searches issue bodies AND comments, not just keywords in titles"
    },
    {
      "id": "gh-api-issues",
      "action": "auto",
      "pattern": "\\bgh\\s+api\\s+\\S*(search/issues|/issues\\b)",
      "hint": "search_knowledge searches issue bodies AND comments, not just keywords in titles",
      "$note": "Free-text query extraction from gh api command strings is not yet implemented; this guard survives for intent-documentation only"
    },
    {
      "id": "glab-issue-list",
      "action": "auto",
      "pattern": "\\bglab\\s+issue\\s+list\\b",
      "hint": "search_knowledge over this repo's issue source finds the duplicate that words it differently",
      "$note": "glab is not installed on this machine; this guard is kept for portability only and is unverified here"
    },
    {
      "id": "gitlab-mcp-issue-trawl",
      "action": "auto",
      "tools": [
        "mcp__gitlab__list_issues",
        "mcp__gitlab__my_issues",
        "mcp__gitlab-gql__search_issues",
        "mcp__gitlab-gql__search_gitlab"
      ],
      "hint": "search_knowledge searches the same issues semantically, over bodies AND comments"
    },
    {
      "id": "gitlab-mcp-note-search",
      "action": "auto",
      "search_only": true,
      "tools": ["mcp__gitlab-gql__search_notes"],
      "hint": "search_knowledge searches issue bodies AND comments semantically, which is what a note search is reaching for"
    }
  ],
  "max_index_age_seconds": 86400
}
