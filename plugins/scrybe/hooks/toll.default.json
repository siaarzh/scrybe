{
  "$comment": "Shipped defaults for the Scrybe search toll. Do NOT edit this file — it is overwritten on every plugin update. Copy the keys you want into <SCRYBE_DATA_DIR>/toll.json instead; that file wins.",
  "enabled": true,
  "lower_seconds": 15,
  "upper_seconds": 30,
  "scope": "global",
  "window": "fixed",
  "marker_dir": null,
  "note_once_per_session": true,
  "guards": [
    {
      "id": "gh-issue-list",
      "action": "toll",
      "pattern": "\\bgh\\s+issue\\s+list\\b",
      "hint": "search_knowledge over this repo's issue source finds the duplicate that words it differently"
    },
    {
      "id": "gh-search-issues",
      "action": "toll",
      "pattern": "\\bgh\\s+search\\s+issues\\b",
      "hint": "search_knowledge searches issue bodies AND comments, not just keywords in titles"
    },
    {
      "id": "gh-api-search-issues",
      "action": "toll",
      "pattern": "\\bgh\\s+api\\s+\\S*search/issues",
      "hint": "search_knowledge searches issue bodies AND comments, not just keywords in titles"
    },
    {
      "id": "glab-issue-list",
      "action": "toll",
      "pattern": "\\bglab\\s+issue\\s+list\\b",
      "hint": "search_knowledge over this repo's issue source finds the duplicate that words it differently"
    },
    {
      "id": "gitlab-mcp-issue-trawl",
      "action": "toll",
      "tools": [
        "mcp__gitlab__list_issues",
        "mcp__gitlab__my_issues",
        "mcp__gitlab-gql__get_issues",
        "mcp__gitlab-gql__search_issues",
        "mcp__gitlab-gql__get_user_issues",
        "mcp__gitlab-gql__list_work_items"
      ],
      "hint": "search_knowledge searches the same issues semantically, over bodies AND comments"
    }
  ]
}
