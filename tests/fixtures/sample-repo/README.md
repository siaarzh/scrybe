# sample-repo

Minimal fixture repository used by scrybe test suite.

## Modules

- `src/alpha.ts` — greeting utilities (`alphaGreeting`, `alphaFarewell`)
- `src/beta.ts` — numeric processing engine (`BetaEngine`)
- `src/gamma.ts` — miscellaneous helpers (`clamp`, `dedupe`, `chunk`, `formatBytes`)
